import styled from "@emotion/styled";
import {
  Container,
  PostHeader,
  PostAddButton,
} from "../community_Header/styles";

export const WriteContainer = styled(Container)``;
export const WriteHeader = styled(PostHeader)``;
export const AddBtn = styled(PostAddButton)``;
